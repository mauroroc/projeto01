<h2>Relação de Vagas</h2>    
    <table>
      <tr>
        <td>Empresa</td>
        <td>Vaga</td>
        <td>Status</td>
      </tr>
      <% @jobs.each do |job| %>
      <% if job.Ativo? && job.expiration >= DateTime.now %> 
      <tr>             
        <td><%= link_to job.company.domain, job.company %>        
        <td><%= link_to job.title, job %></td>                  
        <td><%= job.status %></td>          
      </tr>
      <% end %>
      <% end %>  
    </table>  
<% if employee_signed_in? %>    
    <%= link_to "Cadastrar Vaga", new_job_path %>          
<% end %>